{% extends 'back-office.html.twig' %}


{% block links %} <title>Restaurants</title>{% endblock %}


{% block contenu %}

{% block title %}<ol class="breadcrumb">
    <li class="breadcrumb-item">
        <a href="#">Dashboard</a>
    </li>
    <li class="breadcrumb-item active">Promotion</li>
</ol>{% endblock %}

{% block body %}
    <h1>Promotion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ promotion.id }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ promotion.dateDebut ? promotion.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ promotion.dateFin ? promotion.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>HeureDebut</th>
                <td>{{ promotion.heureDebut ? promotion.heureDebut|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>HeureFin</th>
                <td>{{ promotion.heureFin ? promotion.heureFin|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Banner</th>
                <td><img src="{{ promotion.banner }}"></td>
            </tr>
            <tr>
                <th>PrixPromo</th>
                <td>{{ promotion.prixPromo }}</td>
            </tr>

        </tbody>
    </table>

    <a class="btn_1 gray approve" href="{{ path('promotion_index') }}"><i class="fa fa-fw fa-reply"></i>back to list</a>

    <a class="btn_1 gray approve" href="{{ path('promotion_edit', {'id': promotion.id}) }}">edit</a>

    {{ include('promotion/_delete_form.html.twig') }}


{% endblock %}

{% endblock %}
