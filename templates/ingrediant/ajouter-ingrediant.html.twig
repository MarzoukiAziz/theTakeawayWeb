
{% extends "back-office.html.twig" %}
{% block links %}
    <title>Ajouter un Ingrediant</title>
{%  endblock %}

{% block contenu %}
    <div class="card m-b-20">
        <div class="card-header">
            <h3 cl  ass="card-title">Ajouter un Ingrediant : </h3>

        </div>

        <div class="card-body">
            {{ form_start(f)}}
            <form >
                <div class="form-group">
                    <label class="form-label">Nom</label>
                    {{ form_widget(f.nom , {'attr': {'class': 'form-control form-control-user' , 'placeholder' : 'Numéro'}} ) }}
                    {{ form_errors(f.nom) }}
                </div>
                <div class="form-group">
                    <label class="form-label">Quantite</label>
                    {{ form_widget(f.quantite , {'attr': {'class': 'form-control form-control-user' , 'placeholder' : 'Position Horizontale'}} ) }}
                    {{ form_errors(f.quantite) }}
                </div>
                <div class="form-group">
                    <label class="form-label">Restaurant :</label>
                    {{ form_widget(f.restaurantId , {'attr': {'class': 'form-control form-control-user' , 'placeholder' : 'Positinn Verticale'}} ) }}
                    {{ form_errors(f.restaurantId) }}
                </div>
                <div class="form-group mb-0">
                    <div class="checkbox checkbox-secondary" style="float: right">
                        {{ form_widget(f) }}
                        <button type="SubmitType" class="btn btn-primary " onClick="validateSubmit()">Ajouter</button>
                    </div>
                </div>

            </form>
        </div>
        {{ form_end(f) }}
    </div>
{% endblock %}