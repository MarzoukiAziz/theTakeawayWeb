

{% extends "back-office.html.twig" %}
{% block links %}
    <title>Ajouter un Facture</title>
{%  endblock %}

{% block contenu %}
    <div class="card m-b-20">
        <div class="card-header">
            <h3 cl  ass="card-title">Ajouter un Facture : </h3>

        </div>

        <div class="card-body">
            {{ form_start(f)}}
            <form >
                <div class="form-group">
                    <label class="form-label">quantite</label>
                    {{ form_widget(f.quantite , {'attr': {'class': 'form-control form-control-user' , 'placeholder' : 'Quantite'}} ) }}
                    {{ form_errors(f.quantite) }}
                </div>

                <div class="form-group">
                    <label class="form-label">prixUnitaire :</label>
                    {{ form_widget(f.prixUnitaire , {'attr': {'class': 'form-control form-control-user' , 'placeholder' : 'Prix'}} ) }}
                    {{ form_errors(f.prixUnitaire) }}
                </div>


                <div class="form-group mb-0">
                    <div class="checkbox checkbox-secondary" style="float: right">
                        {{ form_widget(f) }}
                        <button type="SubmitType" class="btn btn-primary " onClick="validateSubmit()">Ajouter</button>
                    </div>
                </div>

            </form>
        </div>
        {{ form_end(f) }}
    </div>
{% endblock %}