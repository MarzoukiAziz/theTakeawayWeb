{% extends "back-office.html.twig" %}
{% block links %}
    <title>Ajouter un Fournisseur</title>
{%  endblock %}

{% block contenu %}
    <div class="card m-b-20">
        <div class="card-header">
            <h3 cl  ass="card-title">Ajouter un Fournisseur : </h3>

        </div>

        <div class="card-body">
            {{ form_start(f)}}
            <form >
                <div class="form-group">
                    <label class="form-label">nom</label>
                    {{ form_widget(f.nom , {'attr': {'class': 'form-control form-control-user' , 'placeholder' : 'Nom'}} ) }}
                    {{ form_errors(f.nom) }}
                </div>
                <div class="form-group">
                    <label class="form-label">adresse</label>
                    {{ form_widget(f.adresse , {'attr': {'class': 'form-control form-control-user' , 'placeholder' : 'Adresse'}} ) }}
                    {{ form_errors(f.adresse) }}
                </div>
                <div class="form-group">
                    <label class="form-label">telephone</label>
                    {{ form_widget(f.telephone , {'attr': {'class': 'form-control form-control-user' , 'placeholder' : 'Telephone'}} ) }}
                    {{ form_errors(f.telephone) }}
                </div>
                <div class="form-group">
                    <label class="form-label">email</label>
                    {{ form_widget(f.email , {'attr': {'class': 'form-control form-control-user' , 'placeholder' : 'Email'}} ) }}
                    {{ form_errors(f.email) }}
                </div>
                <div class="form-group mb-0">
                    <div class="checkbox checkbox-secondary" style="float: right">
                        {{ form_widget(f) }}
                        <button type="SubmitType" class="btn btn-primary " onClick="validateSubmit()">Ajouter</button>
                    </div>
                </div>

            </form>
        </div>
        {{ form_end(f) }}
    </div>
{% endblock %}