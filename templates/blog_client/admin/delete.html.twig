
<form method="post" action="{{ path('blog_admin_delete', {'id': blog_client.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blog_client.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>