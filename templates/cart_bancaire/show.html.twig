{% extends 'base.html.twig' %}

{% block title %}CartBancaire{% endblock %}

{% block body %}
    <h1>CartBancaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cart_bancaire.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ cart_bancaire.numero }}</td>
            </tr>
            <tr>
                <th>Nomcomplet</th>
                <td>{{ cart_bancaire.nomcomplet }}</td>
            </tr>
            <tr>
                <th>Datexp</th>
                <td>{{ cart_bancaire.datexp }}</td>
            </tr>
            <tr>
                <th>Cvv</th>
                <td>{{ cart_bancaire.cvv }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cart_bancaire_index') }}">back to list</a>

    <a href="{{ path('cart_bancaire_edit', {'id': cart_bancaire.id}) }}">edit</a>

    {{ include('cart_bancaire/_delete_form.html.twig') }}
{% endblock %}
